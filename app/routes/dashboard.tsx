import { Outlet } from "react-router"

const Dashboard = () => {
    return (
      <div>
        {""}
        This is a Dashboard Section..
        <Outlet />
      </div>
    )
  }
  
  export default Dashboard